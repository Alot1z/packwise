# act — run GitHub Actions locally (nektos/act)
# https://github.com/nektos/act
# macOS is required to actually compile iOS (xcodebuild only on macOS).
# Linux act can lint workflows but cannot produce an IPA.
-P macos-15=-self-hosted
-P macos=-self-hosted
--container-daemon-socket -
